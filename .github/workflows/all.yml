name: All

on:
  workflow_dispatch:
  schedule:
    - cron: '47 7 * * *'

permissions:
  contents: read
  packages: write

jobs:
  base:
    uses: ./.github/workflows/build-and-push.yml
    with:
      image_name: "base"
  derivatives:
    needs: base
    strategy:
      matrix:
        image_name:
          - "age"
          - "latex"
    uses: ./.github/workflows/build-and-push.yml
    with:
      image_name: ${{ matrix.image_name }}
  antlr:
    uses: ./.github/workflows/build-and-push.yml
    with:
      image_name: "antlr"
    
